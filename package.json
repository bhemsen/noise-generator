{
  "name": "noise-generator-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {"pages": "node scripts/build-pages.mjs", "predev": "npm run pages", "dev": "vite --host 0.0.0.0", "prebuild": "npm run pages", "build": "tsc --noEmit && vite build", "preview": "vite preview --host 0.0.0.0", "check:placeholders": "node scripts/check-placeholders.mjs", "build:deploy": "npm run build && npm run check:placeholders"},
  "devDependencies": {"typescript": "^5.8.3", "vite": "^6.3.5", "vite-plugin-pwa": "^1.0.0"}
}
